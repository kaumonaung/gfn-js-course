console.log('Hallo World');

function printText(text) {
  console.log('Text:', text);
}

printText('Auto');
printText('Haus');

// Um Nodemon global zu installieren:
// npm install -g nodemon

// Um Nodemon lokal zu installieren:
// npm install nodemon
// Dann bekommen wir eine node_modules Ordner und eine package.json und package-lock.json
